spring:
  application:
    name: meeting-room-management
  profiles:
    active: dev
  messages:
    basename: messages
    use-code-as-default-message: true
    encoding: UTF-8
    fallback-to-system-locale: false
  web:
    locale: tr
    locale-resolver: accept_header
  mail:
    host: localhost

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, loggers, circuitbreakers

rate-limit:
  login:
    max-attempts: 5
    window-seconds: 60

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:4173,http://10.104.102.27:4173
